apiVersion: v1
kind: Pod
metadata:
  name: backendpod
  namespace: dev
  labels: 
    environment: dev
    project: roboshop
spec:
  containers:
    - name: nginx
      image: nginx
    - name: os
      image: almalinux:9
      command: ["sleep" , "1000"]